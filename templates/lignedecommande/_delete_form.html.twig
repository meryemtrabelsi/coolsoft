<form method="post" action="{{ path('app_lignedecommande_delete', {'id': lignedecommande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lignedecommande.id) }}">
    <button class="btn">Delete</button>
</form>
