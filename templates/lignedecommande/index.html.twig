{% extends 'base1.html.twig' %}

{% block title %}Lignedecommande index{% endblock %}

{% block body %}
    <h1>Lignedecommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lignedecommande in lignedecommandes %}
            <tr>
                <td>{{ lignedecommande.id }}</td>
                <td>{{ lignedecommande.quantite }}</td>
                <td>
                    <a href="{{ path('app_lignedecommande_show', {'id': lignedecommande.id}) }}">show</a>
                    <a href="{{ path('app_lignedecommande_edit', {'id': lignedecommande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lignedecommande_new') }}">Create new</a>
{% endblock %}
